# Copy to .env (or set in your shell) and edit. The ngrok service in
# docker-compose.yml reads NGROK_AUTHTOKEN from here.
#
# Sign up at https://dashboard.ngrok.com/get-started/your-authtoken
# Free tier is enough for dev/webhook testing.
NGROK_AUTHTOKEN=
